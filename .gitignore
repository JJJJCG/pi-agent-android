*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
/app/build

# Kotlin 2.0 的会话目录
.kotlin/

# 构建产物
*.apk
*.aab
*.ap_
*.hprof

# 正式签名密钥（走 secrets 注入）别进仓库；keystore/nightly.p12 是有意
# 入库的固定侧载密钥（保证 nightly 可覆盖升级），见 app/build.gradle.kts。
*.jks
*.keystore
keystore.properties

# tools/ 下脚本的 Python 字节码缓存
__pycache__/
*.pyc
